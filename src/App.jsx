export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <p className="eyebrow">San Francisco Muslim Community Center</p>
        <h1>Project scaffold is ready.</h1>
        <p className="lede">
          This starter includes React, Vite, and a static `docs` build output so
          the site is ready for the next round of design and content work.
        </p>
      </section>
    </main>
  );
}
